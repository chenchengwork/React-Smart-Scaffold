import { defineMessages, MessageDescriptor } from 'react-intl';

/**
 * 公共信息定义
 */
export const commonLangMsg: {[index: string]: MessageDescriptor} = defineMessages({
    test: {
        id: "test",
        defaultMessage: `test`
    }
});


